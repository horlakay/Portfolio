# Notebooks

This project intentionally keeps core implementation in services and pipelines instead of notebooks. Use this directory only for exploratory analysis that later graduates into code under `training/pipelines/`.

